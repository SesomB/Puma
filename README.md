# Puma

Build instructions:
1) Clone the repo:  ```git clone https://github.com/SesomB/Puma.git --recursive```
2) Create a `build` directory and change into it: ```mkdir build && cd build```
3) Run CMake `cmake ..`
4) Run Make `make`
5) Run Puma ! `sudo ./build/bin/Puma -v trace`
